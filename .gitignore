# Python / notebooks (added later)
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.dbc

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Dataset — never commit
data/raw/*
!data/raw/.gitkeep
data/extracted/*
!data/extracted/.gitkeep
*.csv
*.parquet
*.delta
*.zip

# Run reports (generated from Databricks — safe to commit, row counts only)
# reports/ingestion_latest.json is written by the bronze notebook

# Secrets
.env
*.token

# Personal — local only
Project.md
PROGRESS.md
